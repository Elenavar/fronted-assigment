import { createAction } from '@ngrx/store';

export const enableButtonAdd = createAction('[Button] Enable Button Add');
export const disableButtonAdd = createAction('[Button] Disable Button Add');

export const enableButtonInfo = createAction('[Button] Enable Button Info');
export const disableButtonInfo = createAction('[Button] Disable Button Info');
